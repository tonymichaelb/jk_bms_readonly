const cb=document.createElement('button');cb.textContent='⚙ Configurações';cb.dataset.view='config-view';document.querySelector('nav').append(cb);const cv=document.createElement('section');cv.id='config-view';cv.className='view';cv.innerHTML='<article class="card"><h2>Configurações do BMS</h2><p>Área protegida. O mecanismo oficial de senha e configuração ainda não foi comprovado.</p><input type="password" placeholder="Senha" disabled> <button disabled>Validar acesso</button><p>Leitura e escrita BLE bloqueadas por segurança.</p><div id="config-fields"></div></article>';document.querySelector('main').insertBefore(cv,document.querySelector('.page-footer'));cb.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));cb.classList.add('active');cv.classList.add('active')};fetch('/api/config/settings').then(r=>r.json()).then(x=>document.getElementById('config-fields').innerHTML=x.settings.map(s=>`<p>${s.name}: <b>${s.status}</b></p>`).join(''));
